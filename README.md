# it-challenge-frontend (root)

Pozri detailný návod v `itChallengeFrontend/README.md`.

Rýchle spustenie (z priečinka `itChallengeFrontend`):

```powershell
npm install
echo VITE_API_URL=http://localhost:3000/api > .env
npm run dev
```

Repo tipy:
- Najprv FETCH a PULL pred commitom (vyhneš sa konfliktom)
- Nespúšťaj `npm install` v tomto root priečinku, ale v `itChallengeFrontend`